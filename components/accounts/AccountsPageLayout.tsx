"use client";

/**
 * Accounts page layout shell — breadcrumbs, title, filter card slot, table card.
 * Alias for AccountsPageShell used across all accounts listing/detail screens.
 */

export {
  AccountsPageShell as AccountsPageLayout,
  type AccountsPageShellProps as AccountsPageLayoutProps,
} from "@/components/accounts/AccountsPageShell";
